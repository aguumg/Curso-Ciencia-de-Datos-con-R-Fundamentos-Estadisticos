Para grafico de barras:
 
un data fram es un set de datos, una planilla de datos.

una tabla son esos datos agrupados.

O sea necesita contar los elementos que cumplen cada condición.

Usar el comando class() para ver que clase es el objeto 

data.fram table y no se que otro son parecidos pero no son lo mismo y el codigo de barras precisa una tabla.
